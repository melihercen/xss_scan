# XSS Scanner with Selenium

A Python-based XSS vulnerability scanner that tests for reflected and DOM-based XSS using Selenium WebDriver.


## Installation
1. Clone the repository:
   ```bash
   git clone https://github.com/melihercen/xss_scan.git
   cd xss-scanner
   pip install -r requirements.txt
